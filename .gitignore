# Python virtual environments
venv/
.venv/

# Python bytecode
__pycache__/
*.py[cod]

# Common Python tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# OS / editor noise
.DS_Store
Thumbs.db

# ── Pipeline outputs (regenerated by scripts/new/full_pipeline.py) ──
# These are large and should never be in source control. They are
# rebuilt from data/radical_dataset.csv + cached embeddings.
*.npy
*.npz
cache/
backups/

# Backup files left by integration tests
data/*.bak_*
results/*.bak_*